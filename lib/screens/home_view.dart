import 'package:flutter/material.dart';

import '../helpers/temporary_page.dart';

class HomeView extends StatelessWidget {
  const HomeView({super.key});

  @override
  Widget build(BuildContext context) {
    return TemporaryPage("Home View");
  }
}
